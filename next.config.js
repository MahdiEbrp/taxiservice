/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false,
  i18n: {
    locales: ['en','en-US', 'fa','fa-IR'],
    defaultLocale: 'en',
  },
};

module.exports = nextConfig;
